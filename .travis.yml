sudo: false
language: python

python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6
  - pypy

matrix:
  include:
    - python: 3.5
      script: tox -e flake8
    - python: 2.6
      script: tox -e flake8
      env:
        - IS_PY26=1


install:
  - pip install cython tox

script:
  - tox -e py
